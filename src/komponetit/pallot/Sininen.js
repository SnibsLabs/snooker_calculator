import React from 'react'

export default class Sininen extends React.Component {
    render() {
        return (

                <button id="Sini" className="button sininen" onClick={this.props.Sini}></button>

        )
    }
}
